{
  "private": true,
  "scripts": {
    "dev": "nodemon -w '../src/**/*.ts' -e .ts -x vite dev"
  },
  "devDependencies": {
    "vite": "catalog:vite",
    "vite-plugin-inspect": "catalog:vite"
  }
}
